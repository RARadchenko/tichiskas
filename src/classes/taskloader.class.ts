import { ITaskLoader } from "../interfaces/taskloader.interface";

/**
 * TaskLoader class that implements the ITaskLoader interface.
 * Used to load and manage tasks.
 */
export class TaskLoader extends ITaskLoader {}