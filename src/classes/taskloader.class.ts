import { ATaskLoader } from "../abstracts/taskloader.aclass";

/**
 * TaskLoader class that implements the ITaskLoader interface.
 * Used to load and manage tasks.
 */
export class TaskLoader extends ATaskLoader {}