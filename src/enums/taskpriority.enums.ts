/**
 * Represents the priority levels for a task.
 * - `High` - Indicates a task with the highest priority.
 * - `Default` - Represents a task with normal priority.
 * - `Low` - Denotes a task with the lowest priority.
 */
export enum TaskPriority
{
    High,
    Default,
    Low
}